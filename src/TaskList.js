/** @format */

import TaskItem from './TaskItem';

const TaskList = (props) => {
  return (
    <div>
      <h1>Task List</h1>
      <TaskItem></TaskItem>
    </div>
  );
};

export default TaskList;
